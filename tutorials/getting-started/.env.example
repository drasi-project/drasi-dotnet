# Copy to .env and adjust if you need non-default ports or credentials.
# The app reads these from the environment; defaults match docker-compose.yml.

POSTGRES_HOST=localhost
POSTGRES_PORT=5832
POSTGRES_HOST_PORT=5832
POSTGRES_DATABASE=getting_started
POSTGRES_USER=drasi_user
POSTGRES_PASSWORD=drasi_password

HTTP_SOURCE_PORT=9100
